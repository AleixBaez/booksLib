
services:
    app:
        image: sergio:latest
        command: sh -c "yarn install && yarn run trials"
        env_file:
            - .env_app

        ports:
            - '${APPORT}:${APPORT}'
   